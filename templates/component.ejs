import { Component } from 'react';
import { connect } from 'react-redux';

require('./style.scss');

class <%- name  %> extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div id="<%- id %>" >
      </div>
    )
  }
}

const mapStateToProps = (state) => {
  return {
  }
};

let <%- name %>App = connect(mapStateToProps)(<%- name %>);

export default <%- name %>App;

